{
  "name": "validest",
  "version": "0.4.5",
  "description": "Typed schema validation",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "rm -rf dist && builder && tsc",
    "version:major": "yarn version --new-version major --no-git-tag-version",
    "version:minor": "yarn version --new-version minor --no-git-tag-version",
    "version:patch": "yarn version --new-version patch --no-git-tag-version",
    "publish:major": "yarn publish --access public",
    "publish:minor": "yarn publish --access public",
    "publish:patch": "yarn publish --access public"
  },
  "repository": { "type": "git", "url": "https://github.com/simonboisset/validest", "directory": "packages/validest" },
  "bugs": { "url": "https://github.com/simonboisset/validest/issues" },
  "homepage": "https://github.com/simonboisset/validest",
  "author": "Simon Boisset <simonboisset.dev@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "@validest/and": "0.4.5",
    "@validest/any": "0.4.5",
    "@validest/array": "0.4.5",
    "@validest/core": "0.4.5",
    "@validest/date": "0.4.5",
    "@validest/empty": "0.4.5",
    "@validest/int": "0.4.5",
    "@validest/json": "0.4.5",
    "@validest/nullable": "0.4.5",
    "@validest/number": "0.4.5",
    "@validest/object": "0.4.5",
    "@validest/one-of": "0.4.5",
    "@validest/optional": "0.4.5",
    "@validest/or": "0.4.5",
    "@validest/string": "0.4.5"
  },
  "devDependencies": {
    "builder": "*",
    "esbuild": "^0.14.42",
    "esbuild-node-externals": "^1.4.1",
    "typescript": "^4.7.4"
  },
  "keywords": ["typescript", "validation", "schema"]
}
